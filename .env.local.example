# Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase project settings -> API -> service_role key. Server-only, never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI API key. Server-only, never expose to the client.
OPENAI_API_KEY=
